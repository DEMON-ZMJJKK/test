#include <stdio.h>
int jump(int s,int y)
{
    if(s==0)return y+1;
    else return 2*jump(s-1,y);
}
int main()
{
    int n,s,y;
    while(scanf("%d",&n)!=EOF)
    {
        while(n--)
        {
            scanf("%d %d",&s,&y);
             printf("%d\n",jump(s,y));
        }
    }
    return 0;
}
