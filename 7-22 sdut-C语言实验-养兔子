#include<stdio.h>      
long long int born(int n)
{
    int i;
    long long int a[91];
    a[1]=1;
    a[2]=2;
    for(i=3;i<=n;i++)
    {
       a[i]=a[i-1]+a[i-2];
    }
    return a[n];
}
int main()
{
    int n;
    scanf("%d",&n);
    printf("%lld\n",born(n));
    return 0;
}
