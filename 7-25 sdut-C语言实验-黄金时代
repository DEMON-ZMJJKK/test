#include<stdio.h>
#define q 1/0.61
int a[11];
int f(int n)
{
   int i;
   a[1]=5;
   a[2]=8;
    for(i=3;i<=n;i++)
    {
        a[i]=a[i-1]+a[i-2];
    }
   return a[n];
}
int main()
{
    int n;
    scanf("%d",&n);
    printf("%d\n",f(n));
    return 0;
}
