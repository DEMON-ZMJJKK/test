#include<stdio.h>
 
#include<stdlib.h>
 
int f(int n)
 
{
 
    int i,a[31];
 
    a[1]=1;
 
    for(i=2;i<=n;i++)
 
    {
 
        a[i]=2*(a[i-1]+1);
 
    }
 
    return a[n];
 
}
 
int main()
 
{
 
    int n;
 
    scanf("%d",&n);
 
    printf("%d\n",f(n));
 
    return 0;
 
}
