#include<stdio.h>
long int f(int n)
{
    long int a[51];
    int i;
    a[1]=1;
    a[2]=2;
    for(i=3;i<=n;i++)
    {
        a[i]=a[i-1]+a[i-2];
    }
    return a[n];
}
int main()
{
   int n;
   scanf("%d",&n);
   printf("%ld\n",f(n));
    return 0;
}
